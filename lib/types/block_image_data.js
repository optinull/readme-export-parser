'use strict'

/**
 * ReadMe `[block:image]` data structure, as embedded in their markdown export
 * format. Contains an array of {@link module:readme-export-parser.Image|Image}
 * objects.
 *
 * @typedef {object} module:readme-export-parser.BlockImageData
 * @property {module:readme-export-parser.Image[]} images - array of images
 */
