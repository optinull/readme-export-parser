'use strict'

/**
 * ReadMe `[block:api-header]` data structure, as embedded in their markdown
 * export format. Represents an inline header.
 *
 * @typedef {object} module:readme-export-parser.BlockAPIHeaderData
 * @property {string} title - title as markdown
 */
