'use strict'

/**
 * A single image from a ReadMe `[block:image]` data structure.
 *
 * @see module:readme-export-parser.BlockImageData
 * @typedef {object} module:readme-export-parser.Image
 * @property {Array} image - structure is [URL, filename, ?, ?, bgColor?]
 * @property {string} caption - caption
 */
